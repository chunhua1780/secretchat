name: Build APK
on:
  push:
    branches: [ main ]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
    - name: Setup Android SDK
      uses: android-actions/setup-android@v3
    - name: Fix gradle wrapper
      run: |
        cd platforms/android
        chmod +x gradlew
    - name: Copy www to assets
      run: |
        mkdir -p platforms/android/app/src/main/assets/www
        cp www/index.html platforms/android/app/src/main/assets/www/index.html
        echo "Copied index.html to assets"
        grep "dis:lg" platforms/android/app/src/main/assets/www/index.html | head -2
    - name: Restore native files
      run: |
        cp platforms/android/app/src/main/AndroidManifest.xml platforms/android/app/src/main/AndroidManifest.xml
        mkdir -p platforms/android/app/src/main/java/com/secretchat/app/
    - name: Build APK with Gradle
      run: |
        cd platforms/android
        ./gradlew assembleDebug
    - name: Find APK
      run: find . -name "*.apk"
    - uses: actions/upload-artifact@v4
      with:
        name: GhostChat-APK
        path: '**/*.apk'
        retention-days: 30
